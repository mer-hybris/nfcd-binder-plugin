name=nfcbinder
libdir=/usr/lib
includedir=/usr/include

Name: ${name}
Description: nfcd binder plugin development library
Version: [version]
Requires.private: libglibutil gobject-2.0 gio-2.0 glib-2.0
Libs: -L${libdir} -l:${name}.so
Cflags: -I${includedir} -I${includedir}/${name}
